import AssetInventorySection from '../components/AssetInventorySection'

function AssetInventoryPage({ token }) {
  return <AssetInventorySection token={token} />
}

export default AssetInventoryPage
